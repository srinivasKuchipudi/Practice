Task: Add the logging for the file created in the first task

Here are the task details
1. Log as an warning if the input provided is float (which contains the decimals)
2. If the program executed properly, write a log with the log level "Info"
3. If the program has any other issue, which causes a run time issue, log it as an error, and add the error details 
